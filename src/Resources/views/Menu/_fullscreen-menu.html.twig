<div id="fullscreen-menu" class="hide">

    <div class="main-content main-content-fullscreen">

        <div class="header d-flex align-items-center justify-content-between">
            <a href="{{ url('_admin') }}" class="logo">
                <img src="{{ asset(logoMenuPath) }}" alt="{{ admin_client.name }}" width="{{ logoMenuWidth }}">
            </a>
            <div id="close-fullscreen-menu">
                {{ ux_icon('lucide:circle-x') }}
            </div>
        </div>

        <div class="body">

            <div class="row">

                {{ render_menu() }}
                {{ include('@AropixelAdmin/Menu/_quick-menu.html.twig') }}

            </div>

        </div>

    </div>

</div>
