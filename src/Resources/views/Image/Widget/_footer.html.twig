<div class="caption card-footer">
    {% block caption %}
        {% if image is defined and image %}
            <h6 class="no-margin-top text-semibold form-image{{ required is defined and required ? ' required' : '' }}">{{ image.title ? image.title : 'text.no_title'|trans }}</h6>
            <div>{{ description is defined and description|length ? description : 'text.choose_image'|trans }}</div>
        {% else %}
            <h6 class="no-margin-top text-semibold form-image{{ required is defined and required ? ' required' : '' }}">{{ label is defined and label|length ? label : 'text.featured_image'|trans }}</h6>
            <div>{{ description is defined and description|length ? description : 'text.choose_image'|trans }}</div>
        {% endif %}
    {% endblock %}
</div>
