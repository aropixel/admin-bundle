<div class="caption card-footer">
    {% block caption %}
        {% if image is defined and image %}
            <h6 class="no-margin-top text-semibold form-image{{ required is defined and required ? ' required' : '' }}">{{ image.title ? image.title : 'Sans titre' }}</h6>
            <div>{{ description is defined and description|length ? description : 'Choisissez une image' }}</div>
        {% else %}
            <h6 class="no-margin-top text-semibold form-image{{ required is defined and required ? ' required' : '' }}">{{ label is defined and label|length ? label : 'Image à la une' }}</h6>
            <div>{{ description is defined and description|length ? description : 'Choisissez une image' }}</div>
        {% endif %}
    {% endblock %}
</div>
